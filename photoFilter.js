export default function photoFilter($1) {

   // myFunction();
/* When the user clicks on the button, 
toggle between hiding and showing the dropdown content */
/* function myFunction() {
    document.getElementById("myDropdown").classList.toggle("show");
  }
  
  // Close the dropdown if the user clicks outside of it
  window.onclick = function(event) {
    if (!event.target.matches('.dropbtn')) {
      var dropdowns = document.getElementsByClassName("dropdown-content");
      var i;
      for (i = 0; i < dropdowns.length; i++) {
        var openDropdown = dropdowns[i];
        if (openDropdown.classList.contains('showDD')) {
          openDropdown.classList.remove('showDD');
        }
      }
    }
  } */
  
    /* var selectBox = document.getElementById("photoFilter");
    var selectedValue = selectBox.options[selectBox.selectedIndex].value;
    alert(selectedValue); */

    //document.getElementById("photoFilter").addEventListener("change", photoFilter);
    //alert($i);
    /* var selectBox = document.getElementById("photoFilter");
    var selectedValue = selectBox.options[selectBox.selectedIndex].value;
    alert(selectedValue); */
   
}